Improvement ideas for latex2png:
- ✅ light/dark mode, including downloading with theme-aware background
- ✅ move format into a drop down "sidecar" on the save button.
- ✅ change scale to plus/minus zoom buttons, which also change the preview.
- ✅ flere tilfældige eksempler ved tryk på knap (made "Show example" link cycle through examples)
- ✅ gør noget ved tryk på selve formel. Måske download? (clicking the formula now downloads it)
- ✅ i stedet for "wrap" checkbox, så bare se om der er en fejl, og hvis ikke, så gør det? (implemented smarter auto-wrap that checks for existing environments)

Future ideas:
- Add more LaTeX examples
- Add ability to customize colors for the exported image
- Add option to export with transparent background
- Add keyboard shortcuts for common actions
